\"\"\"Shim router to expose legacy router endpoints (conversations, chat, webhooks, etc.).\"\"\"\n+try:\n+    # prefer multiagent router when enabled via package-level shim\n+    from app.api.chatbot import router as chatbot_pkg_router  # type: ignore\n+    # if the package-level router is an APIRouter (legacy), expose it; otherwise fallback\n+    router = getattr(chatbot_pkg_router, \"router\", chatbot_pkg_router)\n+except Exception:\n+    # fallback to legacy explicit import\n+    from app.api.chatbot.legacy.router.router import router as router  # type: ignore\n+\n*** End Patch
